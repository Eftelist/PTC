package PTC.Helpers

object Permissions {

    private val perm = "ptc."

    var BROADCAST = perm + "broadcast"
    var GAMEMODE = perm + "gamemode"
    var FLY = perm + "fly"
    var SPEED = perm + "speed"
}
